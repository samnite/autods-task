import React from 'react';
import styled from 'styled-components';

const StyledHeader = styled.h1`
  color: #333;
`;

const Header = () => {
  return <StyledHeader>23 Untracked products in my store</StyledHeader>;
};

export default Header;
